import { PaginationDefaultValuesPipe } from './pagination-default-values.pipe';

describe('PaginationDefaultValuesPipe', () => {
  it('should be defined', () => {
    expect(new PaginationDefaultValuesPipe()).toBeDefined();
  });
});
